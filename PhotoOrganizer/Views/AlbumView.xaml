<UserControl
    x:Class="PhotoOrganizer.Views.AlbumView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhotoOrganizer.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:PhotoOrganizer.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    d:DataContext="{d:DesignInstance viewModels:AlbumViewModel}">

    <Grid>

        <TextBlock Text="{Binding NumberOfPhotos}" Style="{StaticResource TitleStyle}" VerticalAlignment="Top" Margin="40"/>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <ProgressRing IsActive="{Binding IsFetchingPhotos}" Foreground="{StaticResource HighHighlightBrush}"
                          Style="{StaticResource HugeProgressRingStyle}"/>
            <TextBlock Text="Fetching Photos" Style="{StaticResource TitleStyle}" Visibility="{Binding IsFetchingPhotos}"/>
        </StackPanel>

    </Grid>

</UserControl>