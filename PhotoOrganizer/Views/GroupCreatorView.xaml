<UserControl
    x:Class="PhotoOrganizer.Views.GroupCreatorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:PhotoOrganizer.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    d:DataContext="{d:DesignInstance viewModels:GroupCreatorViewModel}">

    <Button Style="{StaticResource TransparentButton}"
            Padding="5"
            Command="{Binding GroupLogicCommand}">
        <FontIcon Glyph="&#xECC8;" FontSize="65"/>
    </Button>

</UserControl>